@{
    Layout = "_Layout";
}
@model OOTTracker.Models.ItemAgeRequirements.ItemAgeRequirementsIndexModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

<h2>Item Age Requirements</h2>
<div class="row mt-3">
    <div class="col-3">
        <a class="btn btn-primary" asp-action="Edit">Add Age Requirement</a>
    </div>
</div>
<table data-toggle="table" data-pagination="true" data-page-size="10" data-search="true">
    <thead>
        <tr>
            <th data-width="125">#</th>
            <th data-field="Name" data-sortable="true">Name</th>
        </tr>
    </thead>
    @if (Model.ItemAgeRequirements != null)
    {
        <tbody>
            @foreach (var ageRequirement in Model.ItemAgeRequirements)
            {
                <tr>
                    <td>
                        <a class="btn btn-primary me-2" asp-action="Edit" asp-route-id="@ageRequirement.Id">
                            <i class="bi-pencil"></i>
                        </a>
                        <a class="btn btn-danger" asp-action="Delete" asp-route-id="@ageRequirement.Id">
                            <i class="bi-trash3"></i>
                        </a>
                    </td>
                    <td>@ageRequirement.Name</td>
                </tr>
            }
        </tbody>
    }
</table>